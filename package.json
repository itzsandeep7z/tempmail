{
  "name": "tempmail-buzz",
  "version": "1.0.0",
  "private": true,
  "description": "TempMail Buzz frontend built with React, Vite, and Tailwind CSS",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "postinstall": "npm install --prefix client"
  },
  "engines": {
    "node": ">=18"
  }
}
